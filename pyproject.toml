[tool.ruff]
line-length = 100
target-version = "py312"
exclude = [
    "db/migrations",
    ".venv",
    ".ruff_cache",
]

[tool.ruff.lint]
extend-select = ["B", "E", "F", "W", "I"]
ignore = []

[tool.ruff.lint.isort]
combine-as-imports = true
known-first-party = ["app"]
